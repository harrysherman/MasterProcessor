class D1g1tObject:
    def __init__(self, id, fields_dict):
        """
        Initializes a D1g1tObject instance.

        Args:
            id (int): The ID of the object.
            fields_dict (dict): A dictionary containing the fields of the object.

        Returns:
            None
        """
        self.id = id
        self.fields_dict = fields_dict
